[package]
name = "py-hotstart"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = { version = "1", features = ["backtrace"] }
nix = { version = "0.29", features = ["term", "socket", "uio", "fs", "poll", "signal", "ioctl"] }
signal-hook = "0.3"
